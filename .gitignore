# Original, unoptimized source photos — not served by the site.
# The resized/cropped versions actually used live in assets/photos/.
pictures/

# OS junk
desktop.ini
Thumbs.db
.DS_Store

# Claude Code local settings (may contain machine-specific permissions)
.claude/settings.local.json
